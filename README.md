# WebMobile

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 8.3.17.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The app will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory. Use the `--prod` flag for a production build.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via [Protractor](http://www.protractortest.org/).

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI README](https://github.com/angular/angular-cli/blob/master/README.md).



ng build --prod && docker build -t opinionatedstack/web-angular:latest -f Dockerfile.prod . && docker push opinionatedstack/web-angular:latest && say done


## To get Nativescript running 
install @nativescript/schematics@10.0.2 (10.1.0 has a bug for now)
ng new project
cd 
ng add @nativescript/schematics --skipAutoGeneratedComponent
---
create App_Resruoces/iOS/Podfile with following content
Based on https://github.com/react-native-market/firebase-authentication-kit/issues/4
Oct 5 2019 comment

post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings.delete 'IPHONEOS_DEPLOYMENT_TARGET'
    end
  end
end
----
Change package.json scripts to
based on https://github.com/NativeScript/nativescript-schematics/issues/290
August 1, 2020 comment

    "android": "nsc debug android --no-hmr",
    "ios": "nsc debug ios --no-hmr",
---------

Resolve:
Unable to apply changes on device: EA1FD166-1205-41EC-80A7-59ECEC0A1F72. Error is: ENOENT: no such file or directory, scandir '/Users/patrick/Library/Developer/CoreSimulator/Devices/EA1FD166-1205-41EC-80A7-59ECEC0A1F72/data/Applications'.

by creating folder "Applications" in:

'/Users/patrick/Library/Developer/CoreSimulator/Devices/EA1FD166-1205-41EC-80A7-59ECEC0A1F72/data'

--------
tsconfig.app.json
Can't find locally declared types=>
src/app/services/stripe-payments/stripe-payments.service.ts:10:16 - error TS2304: Cannot find name 'Stripe'.


  "include": [
    "src/**/*.d.ts"
  ],
